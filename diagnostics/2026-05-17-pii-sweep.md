---
type: pii-sweep
business: praxisverify
generated: 2026-05-17
tags: [pii-sweep, security, automated, regkb]
---

# PII / Secret Sweep — RegulatoryKB — 2026-05-17

## Summary

| Category | Count | Severity |
|---|---|---|
| Gitleaks credential findings — working tree | 0 | ✅ Clean |
| Gitleaks credential findings — history (48 commits scanned) | 0 | ✅ Clean |
| Email-pattern hits (non-agency domains) | 16 | ⚠️ Review |
| Unique non-agency email domains | 4 | ⚠️ Review |
| Files with email-pattern hits | 7 | ⚠️ Review |

No API keys, tokens, passwords, or other credential patterns were detected by gitleaks in either the working tree or full commit history (48 commits). All 16 hits are email addresses and fall into two categories: hardcoded configuration values and example/placeholder text in documentation and setup scripts.

---

## Gitleaks Working-Tree Findings (Top 10)

*No findings. Gitleaks exited 0.*

---

## Gitleaks History Findings (Top 10)

*No findings. 48 commits scanned. Gitleaks exited 0.*

---

## Email-Pattern Findings — Top 10 Non-Agency Domains

Agency domains (`@fda.hhs.gov`, `@ec.europa.eu`, `@mhra.gov.uk`, `@emea.europa.eu`, `@gov.ie`, `@hpra.ie`) were excluded before counting.

| Rank | Domain | Hit count | Nature |
|---|---|---|---|
| 1 | `*@vertigenius.com` | 7 | Operator/business domain — hardcoded in `config.yaml` (recipient), `emailer.py` (default sender fallback), and PRD design doc examples |
| 2 | `*@gmail.com` | 6 | Personal/example Gmail — appears as `SMTP_USERNAME` / `IMAP_USERNAME` placeholder text in `README.md`, `scheduler.py`, and `intel.py` setup instructions |
| 3 | `*@donlonlsc.com` | 2 | Personal/business domain — hardcoded as email `sender` in `config/config.yaml` |
| 4 | `*@test.com` | 1 | Test fixture — `tests/test_web_search_resolver.py`; appropriate for test context |

> **GDPR redaction:** local-parts of all addresses are withheld from this report. Only `@domain` portions are shown, as required.

---

## Top 10 File Paths by Email-Pattern Hit Count

| Hits | File |
|---|---|
| 5 | `docs/PRD_Regulatory_Intelligence_Agent.md` |
| 3 | `config/config.yaml` |
| 2 | `scripts/regkb/intelligence/scheduler.py` |
| 2 | `scripts/regkb/commands/intel.py` |
| 2 | `README.md` |
| 1 | `tests/test_web_search_resolver.py` |
| 1 | `scripts/regkb/intelligence/emailer.py` |

---

## Finding Analysis

### HIGH — Real addresses hardcoded in `config/config.yaml`

`config/config.yaml` contains literal email addresses for `intelligence.email.sender` and `intelligence.email.recipients`. This file is committed to the repository. Anyone with read access to the repo can see the operator's personal/business email addresses. If recipients are added or changed, they would appear in git history indefinitely.

**Affected domains:** `*@donlonlsc.com`, `*@vertigenius.com`

### MEDIUM — Hardcoded default in `scripts/regkb/intelligence/emailer.py`

`emailer.py` contains a hardcoded fallback email address used when config is absent. This embeds a real operator address in source code.

**Affected domain:** `*@vertigenius.com`

### LOW — Example addresses in setup documentation

`README.md`, `scripts/regkb/intelligence/scheduler.py`, and `scripts/regkb/commands/intel.py` use Gmail-style example addresses to illustrate environment variable setup (e.g. `set SMTP_USERNAME=...@gmail.com`). These read as real addresses rather than clearly-marked placeholders.

**Affected domain:** `*@gmail.com`

### INFORMATIONAL — PRD design doc

`docs/PRD_Regulatory_Intelligence_Agent.md` contains example configuration snippets and ASCII-art UI mockups with the operator's business email. This is a design artifact; assess whether it should remain in the public repo.

**Affected domain:** `*@vertigenius.com`

### INFORMATIONAL — Test fixture

`tests/test_web_search_resolver.py` uses `*@test.com` as a fixture value. This is appropriate and expected.

---

## Suggested Next Actions

| Priority | Action |
|---|---|
| **1 — High** | Move `intelligence.email.sender` and `intelligence.email.recipients` out of `config/config.yaml` and into `.env` / environment variables (`SMTP_SENDER`, `EMAIL_RECIPIENTS`). Update `config.py` and `emailer.py` to read from env. This prevents email addresses entering git history on any future update. |
| **2 — High** | Remove the hardcoded fallback email from `emailer.py`. Replace with `None` or raise a `ConfigurationError` if no sender is configured. |
| **3 — Medium** | Replace example Gmail addresses in `README.md`, `scheduler.py`, and `intel.py` with clearly-marked placeholders (`your-smtp-account@gmail.com` or `<smtp-user@example.com>`). |
| **4 — Low** | Evaluate whether `docs/PRD_Regulatory_Intelligence_Agent.md` should be removed from the repo or have its example addresses replaced with placeholders before the repo becomes more widely shared. |
| **5 — Ongoing** | Re-run this sweep weekly. Consider adding a `.gitleaks.toml` allowlist for `@test.com` fixture addresses to reduce noise in future reports. |

---

## Methodology

| Step | Tool / Command | Scope |
|---|---|---|
| Credential scan — working tree | gitleaks 8.21.2 `--no-git` | All files in working directory |
| Credential scan — history | gitleaks 8.21.2 `--log-opts="-n 200"` | 48 commits (full history) |
| Email pattern grep | `grep -rEn` with PCRE-style pattern | `*.md *.py *.json *.yml *.yaml *.txt` |
| Agency-domain exclusion | `grep -vE` post-filter | `@fda.hhs.gov @ec.europa.eu @mhra.gov.uk @emea.europa.eu @gov.ie @hpra.ie` |

*This report was generated by the automated weekly PII sweep trigger. No full email addresses or secrets are stored in this file.*
