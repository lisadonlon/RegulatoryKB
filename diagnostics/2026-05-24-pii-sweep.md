---
type: pii-sweep
business: praxisverify
generated: 2026-05-24
tags: [pii-sweep, security, automated, regkb]
---

# RegulatoryKB PII / Secret Sweep — 2026-05-24

## Summary Table

| Category | Count | Severity |
|---|---|---|
| Gitleaks working-tree findings | 0 | — |
| Gitleaks history findings (48 commits scanned) | 0 | — |
| Email-pattern hits (non-agency domains) | 16 | — |
| Unique non-agency email domains detected | 4 | — |
| Files with email-pattern hits | 7 | — |
| **Operational config with hardcoded addresses** | **3** | **HIGH** |
| Real addresses in doc examples | 3 | MEDIUM |
| Safe test placeholders (`@test.com`) | 1 | LOW |

---

## Gitleaks — Working-Tree Scan

Tool: gitleaks 8.21.2 · mode: `--no-git` (all tracked files)

**Result: 0 findings.** No secrets, API keys, tokens, or credential patterns detected in the working tree.

---

## Gitleaks — History Scan (Last 200 Commits)

Commits actually scanned: **48** (repository age; fewer than 200 commits in history).

**Result: 0 findings.** No secrets detected across commit history.

---

## Email-Pattern Analysis

Agency domains excluded from analysis: `fda.hhs.gov`, `ec.europa.eu`, `mhra.gov.uk`, `emea.europa.eu`, `gov.ie`, `hpra.ie`

### Top 10 Non-Agency Email Domains

| Domain | Occurrences | Risk Assessment |
|---|---|---|
| `@vertigenius.com` | 7 | MEDIUM — real business domain; appears in operational config and design docs |
| `@gmail.com` | 6 | MEDIUM — real addresses in README examples and hardcoded config defaults |
| `@donlonlsc.com` | 2 | HIGH — internal/owner domain hardcoded in committed operational config |
| `@test.com` | 1 | LOW — safe test placeholder |

### Top 10 Files by Email Hit Count

| Hits | File Path |
|---|---|
| 5 | `docs/PRD_Regulatory_Intelligence_Agent.md` |
| 3 | `config/config.yaml` |
| 2 | `scripts/regkb/intelligence/scheduler.py` |
| 2 | `scripts/regkb/commands/intel.py` |
| 2 | `README.md` |
| 1 | `tests/test_web_search_resolver.py` |
| 1 | `scripts/regkb/intelligence/emailer.py` |

---

## Findings Detail

> All email local-parts are redacted. No line numbers are paired with email domains.

### Finding 1 — Operational Email Addresses in Committed Config (HIGH)

**File:** `config/config.yaml`

The intelligence pipeline email section contains hardcoded sender and recipient addresses (domains: `@donlonlsc.com`, `@vertigenius.com`). These are real operational addresses committed to source control. If the repository is shared or made public, these addresses will be exposed.

**Risk:** PII exposure; spam-harvesting target; violates separation of config from code.

**Recommendation:** Move `intelligence.email.sender` and `intelligence.email.recipients` to `.env` (e.g. `REGKB_EMAIL_SENDER`, `REGKB_EMAIL_RECIPIENTS`). The `.env` file is already in `.gitignore` for secrets. Reference via environment variables in `config.yaml` or load them through the `Config` singleton.

---

### Finding 2 — Real Email Addresses in README `.env` Examples (MEDIUM)

**File:** `README.md`

The SMTP/IMAP setup section includes what appear to be real Gmail addresses in code-block examples for `SMTP_USERNAME` and `IMAP_USERNAME`. Documentation examples should use clearly fictional placeholders.

**Risk:** Real address leaked in publicly readable documentation; appears as `sk-...` placeholder is used for the API key but the email field is not anonymised to the same standard.

**Recommendation:** Replace with unambiguous placeholders such as `your-address@gmail.com` or `digest-sender@example.com`.

---

### Finding 3 — Real Addresses in Design Document (MEDIUM)

**File:** `docs/PRD_Regulatory_Intelligence_Agent.md`

The PRD contains `@vertigenius.com` addresses (5 occurrences) embedded in architecture tables and example configuration blocks. PRDs are sometimes shared externally during procurement or audit.

**Recommendation:** Redact real addresses from design documents; use role labels (e.g. `regulatory-team@<domain>`) or `@example.com` substitutes.

---

### Finding 4 — Hardcoded Defaults in Source Files (MEDIUM)

**Files:** `scripts/regkb/intelligence/scheduler.py`, `scripts/regkb/commands/intel.py`, `scripts/regkb/intelligence/emailer.py`

These modules contain `@gmail.com` and `@vertigenius.com` addresses as inline defaults or example strings. Even if overridden at runtime, hardcoded addresses add unnecessary exposure surface.

**Recommendation:** Replace with `""` (empty string) defaults and require addresses to be supplied through config or environment. Raise a descriptive error if the operational address is missing rather than falling back to a hardcoded value.

---

### Finding 5 — Test Placeholder (LOW / Informational)

**File:** `tests/test_web_search_resolver.py`

One occurrence of `@test.com`. This is an IANA-reserved example domain and constitutes no real PII exposure. No action required.

---

## ANTHROPIC_API_KEY Pattern Note

The README documents `ANTHROPIC_API_KEY=sk-...` as an environment variable to be placed in `.env`. Gitleaks confirmed no actual key value appears in any tracked file. The `sk-...` shown is a placeholder pattern only. No action required.

---

## Next Actions

| Priority | Action | Owner |
|---|---|---|
| HIGH | Move hardcoded sender/recipients from `config/config.yaml` to `.env` variables; update `Config` to read them from env | Dev |
| MEDIUM | Sanitise `README.md` SMTP/IMAP example blocks — replace real addresses with `@example.com` placeholders | Dev |
| MEDIUM | Redact real addresses from `docs/PRD_Regulatory_Intelligence_Agent.md` | Dev |
| MEDIUM | Remove inline address defaults from `scheduler.py`, `intel.py`, `emailer.py`; fail fast if not configured | Dev |
| LOW | Add `@donlonlsc.com` and `@vertigenius.com` to gitleaks allowlist **only** if they must remain in config; otherwise treat removal as the fix | Dev |
| ONGOING | Run this sweep weekly; consider adding gitleaks as a pre-commit hook (`gitleaks protect`) | Dev/Ops |

---

## Methodology

- **Tool:** gitleaks 8.21.2 (rules: default ruleset)
- **Working-tree scan:** `gitleaks detect --no-git`
- **History scan:** `gitleaks detect --log-opts="-n 200"` (48 commits available)
- **Email grep:** `grep -rEn` across `*.md *.py *.json *.yml *.yaml *.txt`; agency domains excluded
- **GDPR note:** All email local-parts are redacted throughout this report. No local-part is recorded. Line numbers are not paired with domain names.
- **Scope:** Source files only; `.venv/`, `node_modules/`, `archive/`, `extracted/` excluded.

*Generated by automated weekly PII sweep · RegulatoryKB · 2026-05-24*
