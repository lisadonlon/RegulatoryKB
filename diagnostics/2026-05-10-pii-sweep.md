---
type: pii-sweep
business: praxisverify
generated: 2026-05-10
tags: [pii-sweep, security, automated, regkb]
---

# RegulatoryKB PII / Secret Sweep — 2026-05-10

## Summary Table

| Check | Tool | Findings |
|-------|------|----------|
| Working-tree secret scan | gitleaks 8.30.1 | **0** |
| Git history secret scan (last 48 commits) | gitleaks 8.30.1 | **0** |
| Email-pattern hits (non-agency domains) | grep regex | **16 hits across 7 files** |
| Unique non-agency email domains found | domain grep | **4 domains** |

No API keys, tokens, passwords, or structured secrets detected by gitleaks in either the working tree or git history.

---

## Top 10 Working-Tree Gitleaks Findings

*No findings. Gitleaks reported 0 leaks in working tree scan.*

---

## Top 10 History Gitleaks Findings

*No findings. Gitleaks scanned 48 commits and reported 0 leaks.*

> Note: The repository has 48 commits total; the scan covered all of them (--log-opts="-n 200" requested, 48 available).

---

## Top 10 Non-Agency Email-Pattern Domains

Agency domains excluded from this list: `fda.hhs.gov`, `ec.europa.eu`, `mhra.gov.uk`, `emea.europa.eu`, `gov.ie`, `hpra.ie`

| Rank | Domain | Hit Count | Assessment |
|------|--------|-----------|------------|
| 1 | `*@vertigenius.com` | 7 | **Medium** — business email in committed config + PRD doc |
| 2 | `*@gmail.com` | 6 | Low — placeholder examples in README / CLI help / batch script comments |
| 3 | `*@donlonlsc.com` | 2 | **Medium** — business email committed to `config/config.yaml` |
| 4 | `*@test.com` | 1 | Benign — test fixture only |

---

## Top 10 File Paths by Email-Pattern Hit Count

| Hit Count | File Path |
|-----------|-----------|
| 5 | `docs/PRD_Regulatory_Intelligence_Agent.md` |
| 3 | `config/config.yaml` |
| 2 | `scripts/regkb/intelligence/scheduler.py` |
| 2 | `scripts/regkb/commands/intel.py` |
| 2 | `README.md` |
| 1 | `tests/test_web_search_resolver.py` |
| 1 | `scripts/regkb/intelligence/emailer.py` |

---

## Finding Detail

### MEDIUM — Business email addresses committed to `config/config.yaml`

**Files:** `config/config.yaml` (lines 289, 292–293)

The SMTP `sender` and `recipients` fields in the committed configuration file contain real business email addresses (`*@donlonlsc.com`, `*@vertigenius.com`). This file is version-controlled and pushes these addresses into the repository's history. These are production routing addresses, not credentials, but their presence in a committed config file violates the principle of keeping personal/business contact information out of source code.

**Risk level:** Medium — no passwords or API keys exposed; addresses are internal routing contacts, not credentials. However, they add a GDPR data-minimisation concern for any contributor who forks or clones the repo.

---

### MEDIUM — Hardcoded default email fallback in `emailer.py`

**File:** `scripts/regkb/intelligence/emailer.py`

A `*@vertigenius.com` address appears as a default argument fallback in source code, meaning it would be used if configuration is absent. This couples a business email address to the codebase itself.

**Risk level:** Medium — same as above; no credential exposure, but a business contact embedded in source.

---

### LOW — Placeholder email examples in documentation and CLI help

**Files:** `README.md`, `scripts/regkb/intelligence/scheduler.py`, `scripts/regkb/commands/intel.py`

These occurrences are all `*@gmail.com` patterns appearing in:
- README `.env` setup examples (commented-out)
- Generated Windows batch script REM comments
- `click.echo()` CLI setup instructions

These are instructional placeholders, not real credentials or contact data.

**Risk level:** Low — no real PII; generic provider placeholder.

---

### LOW — PRD design document examples

**File:** `docs/PRD_Regulatory_Intelligence_Agent.md`

The product requirements document contains `*@vertigenius.com` in example config blocks and ASCII-art UI mockups. These mirror the values in `config/config.yaml` and reflect the same business email concern, but in a design artefact rather than operational code.

**Risk level:** Low-Medium — PRD is documentation; concern is the same as config.yaml.

---

### BENIGN — Test fixture

**File:** `tests/test_web_search_resolver.py`

`*@test.com` appears in a test fixture as a `requester_email` field. This is a synthetic address with no real-world PII.

**Risk level:** None.

---

## Suggested Next Actions

| Priority | Action | Owner |
|----------|--------|-------|
| **1 — High** | Move `config/config.yaml` email `sender` and `recipients` to environment variables (`SMTP_SENDER`, `SMTP_RECIPIENTS`) and remove committed values; update `config.py` to read from env with blank defaults | Engineering |
| **2 — Medium** | Replace the hardcoded `*@vertigenius.com` fallback in `emailer.py` with `""` or raise a clear `ConfigurationError` if no sender is configured | Engineering |
| **3 — Medium** | Redact or genericise email addresses in `docs/PRD_Regulatory_Intelligence_Agent.md` (e.g. use `sender@example.com`, `recipient@example.com`) | Documentation |
| **4 — Low** | Consider adding `config/config.yaml` email fields to `.gitignore` exclusion or documenting that they must be blanked before commit | DevOps |
| **5 — Ongoing** | Re-run this sweep weekly; add `*@donlonlsc.com` and `*@vertigenius.com` to a gitleaks allowlist entry if these domains are intentional org identifiers, with a comment explaining the decision | Security |

---

## Methodology

- **Gitleaks version:** 8.30.1
- **Working tree scope:** all files in repo root, no-git mode
- **History scope:** last 200 commits requested; 48 commits available (all scanned)
- **Email grep:** regex `\b[A-Za-z0-9._-]+@[A-Za-z0-9.-]+\.(ie|com|co\.uk|eu|org)\b` against `*.md`, `*.py`, `*.json`, `*.yml`, `*.yaml`, `*.txt`
- **Agency exclusions applied:** fda.hhs.gov, ec.europa.eu, mhra.gov.uk, emea.europa.eu, gov.ie, hpra.ie
- **GDPR redaction:** all email local-parts omitted from this report; domain-level data only; no line numbers paired with email patterns
- **Excluded paths:** .venv, node_modules

*Generated by automated weekly PII sweep trigger.*
