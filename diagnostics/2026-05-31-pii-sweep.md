---
type: pii-sweep
business: praxisverify
generated: 2026-05-31
tags: [pii-sweep, security, automated, regkb]
---

# PII / Secret Sweep — RegulatoryKB — 2026-05-31

## Summary

| Check | Tool | Scope | Findings |
|-------|------|-------|----------|
| Secret / credential scan — working tree | gitleaks 8.21.2 | All tracked files | **0** |
| Secret / credential scan — git history | gitleaks 8.21.2 | Last 48 commits (full history) | **0** |
| Email-pattern scan | grep + domain filter | `*.md, *.py, *.json, *.yml, *.yaml, *.txt` | **16 hits · 7 files** |
| Unique non-agency email domains | — | Agency domains excluded¹ | **4 domains** |

¹ Excluded agency domains: `fda.hhs.gov`, `ec.europa.eu`, `mhra.gov.uk`, `emea.europa.eu`, `gov.ie`, `hpra.ie`

**Overall risk level: LOW–MEDIUM.** No secrets or credentials detected by gitleaks. The email-pattern hits are a mix of hardcoded config addresses (medium risk), documentation examples (low risk), and test fixtures (negligible risk).

---

## Working-Tree Gitleaks Findings (Top 10)

*None.* Gitleaks found 0 findings across all working-tree files.

---

## History Gitleaks Findings (Top 10)

*None.* 48 commits scanned. 0 findings.

---

## Top 10 Non-Agency Email-Pattern Domains

Agency domains (`fda.hhs.gov`, `ec.europa.eu`, `mhra.gov.uk`, `emea.europa.eu`, `gov.ie`, `hpra.ie`) were excluded from this count.

| Rank | Domain | Hit Count | Assessment |
|------|--------|-----------|------------|
| 1 | `*@vertigenius.com` | 7 | **Medium** — Appears in committed config and code defaults; appears to be a real business domain |
| 2 | `*@gmail.com` | 6 | **Low** — Used as example/placeholder in README, CLI help text, and Windows batch comments |
| 3 | `*@donlonlsc.com` | 2 | **Medium** — Business domain email addresses hardcoded in `config/config.yaml` as sender/recipient |
| 4 | `*@test.com` | 1 | **Negligible** — Test fixture only |

---

## Top 10 File Paths by Email-Pattern Hit Count

| Hit Count | File Path |
|-----------|-----------|
| 5 | `docs/PRD_Regulatory_Intelligence_Agent.md` |
| 3 | `config/config.yaml` |
| 2 | `scripts/regkb/intelligence/scheduler.py` |
| 2 | `scripts/regkb/commands/intel.py` |
| 2 | `README.md` |
| 1 | `tests/test_web_search_resolver.py` |
| 1 | `scripts/regkb/intelligence/emailer.py` |

*All email local-parts are redacted. Line numbers are not paired with domain findings per GDPR policy.*

---

## Finding Detail

### config/config.yaml — MEDIUM risk

Real business email addresses (`*@donlonlsc.com` and `*@vertigenius.com`) are hardcoded in the tracked config file as SMTP sender and digest recipient values. These are committed to the git history and visible to anyone with repository access. GDPR/data-minimisation concern: recipient email addresses of identifiable individuals should not be in version-controlled config.

### scripts/regkb/intelligence/emailer.py — MEDIUM risk

A `*@vertigenius.com` address is used as a hardcoded fallback default in the code. If the config key is missing, this address is silently used, which may be unexpected.

### docs/PRD_Regulatory_Intelligence_Agent.md — LOW risk

PRD document contains `*@vertigenius.com` in example YAML snippets and simulated output blocks. These appear to be illustrative examples in design documentation rather than operational addresses, but they still record a real domain name in a committed document.

### README.md — LOW risk

Uses `*@gmail.com` as placeholder format examples in environment-variable setup instructions. This is standard documentation practice with a generic provider and not tied to a specific individual.

### scripts/regkb/commands/intel.py — LOW risk

CLI `setup` command echoes `*@gmail.com` as a format hint in `click.echo()` strings. Acceptable usage pattern.

### scripts/regkb/intelligence/scheduler.py — LOW risk

Windows batch script template contains `REM` comment lines showing `*@gmail.com` as example values. Low risk; comments only.

### tests/test_web_search_resolver.py — NEGLIGIBLE risk

Test fixture uses `*@test.com`, a clearly synthetic domain with no real-world registration. No action required.

---

## Suggested Next Actions

| Priority | Action | File(s) |
|----------|--------|---------|
| **High** | Move hardcoded sender/recipient email addresses out of `config/config.yaml` to `.env` (already in `.gitignore`); replace config values with environment variable references | `config/config.yaml` |
| **Medium** | Remove hardcoded fallback email address in `emailer.py`; raise a clear error or use `None` if the config key is absent | `scripts/regkb/intelligence/emailer.py` |
| **Medium** | Review `docs/PRD_Regulatory_Intelligence_Agent.md` — replace real-domain examples with `user@example.com` (RFC 2606 reserved domain) | `docs/PRD_Regulatory_Intelligence_Agent.md` |
| **Low** | Replace `*@gmail.com` example strings in README and CLI help text with `user@example.com` to avoid creating the impression that a specific provider is required | `README.md`, `scripts/regkb/commands/intel.py`, `scripts/regkb/intelligence/scheduler.py` |
| **Informational** | Consider expanding the agency-domain exclusion list in the grep filter to cover additional known regulatory agencies (e.g., `who.int`, `tga.gov.au`, `pmda.go.jp`) to reduce false positives in future sweeps | Sweep config |

---

## Scan Parameters

| Parameter | Value |
|-----------|-------|
| Scan date | 2026-05-31 |
| gitleaks version | 8.21.2 |
| History depth | 48 commits (all available; requested 200) |
| File extensions scanned | `*.md *.py *.json *.yml *.yaml *.txt` |
| Excluded paths | `.venv/`, `node_modules/` |
| Excluded agency domains | `fda.hhs.gov`, `ec.europa.eu`, `mhra.gov.uk`, `emea.europa.eu`, `gov.ie`, `hpra.ie` |
| GDPR redaction applied | Yes — local-parts redacted; line numbers not paired with email domains |

---

*Generated by automated weekly PII sweep. All email addresses are domain-only (local-parts redacted).*
